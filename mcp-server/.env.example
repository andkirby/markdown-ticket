# MCP Server Configuration
# Copy this file to .env and adjust values as needed

# ===== Core Configuration =====
# HTTP Transport
MCP_HTTP_ENABLED=false          # Enable HTTP transport (default: false, stdio always enabled)
MCP_HTTP_PORT=3002              # HTTP port (default: 3002)
MCP_BIND_ADDRESS=127.0.0.1      # Bind address: 127.0.0.1 (local) or 0.0.0.0 (Docker)

# Logging
MCP_LOG_LEVEL=info              # Log level: debug, info, warn, error (default: info)

# Project Discovery
MCP_CACHE_TIMEOUT=300           # Cache timeout in seconds (default: 300)

# ===== Phase 2: Advanced Features (All Optional) =====
# All features disabled by default for simplicity

# Session Management
MCP_SESSION_TIMEOUT_MS=1800000  # Session timeout in milliseconds (default: 1800000 = 30 minutes)

# Origin Validation (DNS rebinding protection)
MCP_SECURITY_ORIGIN_VALIDATION=false      # Enable origin validation (default: false)
MCP_ALLOWED_ORIGINS=http://localhost:5173 # Comma-separated allowed origins (if validation enabled)

# Rate Limiting
MCP_SECURITY_RATE_LIMITING=false          # Enable rate limiting (default: false)
MCP_RATE_LIMIT_MAX=100                    # Max requests per window (default: 100)
MCP_RATE_LIMIT_WINDOW_MS=60000            # Time window in milliseconds (default: 60000 = 1 minute)

# Authentication
MCP_SECURITY_AUTH=false                   # Enable authentication (default: false)
MCP_AUTH_TOKEN=                           # Auth token (Bearer token, required if auth enabled)

# ===== Example Configurations =====

# Local Development (stdio only):
# MCP_HTTP_ENABLED=false
# MCP_LOG_LEVEL=info

# Local Development (with HTTP, no security):
# MCP_HTTP_ENABLED=true
# MCP_HTTP_PORT=3002
# MCP_BIND_ADDRESS=127.0.0.1
# MCP_LOG_LEVEL=info

# Local Development (with HTTP and all Phase 2 features):
# MCP_HTTP_ENABLED=true
# MCP_HTTP_PORT=3002
# MCP_BIND_ADDRESS=127.0.0.1
# MCP_LOG_LEVEL=debug
# MCP_SESSION_TIMEOUT_MS=3600000
# MCP_SECURITY_ORIGIN_VALIDATION=true
# MCP_ALLOWED_ORIGINS=http://localhost:5173
# MCP_SECURITY_RATE_LIMITING=true
# MCP_RATE_LIMIT_MAX=50
# MCP_RATE_LIMIT_WINDOW_MS=60000

# Docker Deployment (production with security):
# MCP_HTTP_ENABLED=true
# MCP_HTTP_PORT=3002
# MCP_BIND_ADDRESS=0.0.0.0
# MCP_LOG_LEVEL=warn
# MCP_SESSION_TIMEOUT_MS=1800000
# MCP_SECURITY_ORIGIN_VALIDATION=true
# MCP_ALLOWED_ORIGINS=https://your-app.com,https://www.your-app.com
# MCP_SECURITY_RATE_LIMITING=true
# MCP_RATE_LIMIT_MAX=100
# MCP_RATE_LIMIT_WINDOW_MS=60000
# MCP_SECURITY_AUTH=true
# MCP_AUTH_TOKEN=your-secure-random-token-here
