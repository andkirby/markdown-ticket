/**
 * UI Configuration
 *
 * Centralized UI-related constants and configuration values.
 */

/**
 * The icon/emoji used to represent worktree status across the UI.
 * Default: 🪾 (empty deciduous tree emoji)
 */
export const WORKTREE_ICON = '🪾'

/**
 * UI Label Constants
 */
export const UI_LABELS = {
  WORKTREE: 'Worktree',
  WORKTREE_BADGE: `${WORKTREE_ICON} Worktree`,
} as const
