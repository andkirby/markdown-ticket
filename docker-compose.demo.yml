x-shared-volumes: &project-volumes
    volumes:
      - ./debug-tasks:/projects/debug-tasks
      - ./:/projects/markdown-ticket

services:
  backend:
    <<: *project-volumes
  mcp:
    <<: *project-volumes

