# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*

# Build outputs
dist/
build/
.next/
.nuxt/
**/*.js
**/*.d.ts
**/*.js.map
**/*.d.ts.map
!/server/**/*.js
# DSH plugin glue is source, not build output (MDT-143).
!/dsh-plugin/*.js
# Generated Cloudflare Worker binding types (MDT-200 cloud/ workspace).
# Committed as the binding contract per docs/architecture/cloud-sync/operations.md.
!/cloud/worker-configuration.d.ts

# Docker files (not ignored)
docker-compose.*.yml
#docker-compose.override.yml
!/Dockerfile.frontend
!/nginx.conf
!/docker-compose.yml
!/docker-compose.dev.yml
!/docker-compose.prod.yml
!/.dockerignore
!/README.docker.md

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Testing
coverage/
.playwright-report/
.nyc_output/
junit.xml

# Logs
*.log

# Temporary files
tmp/
temp/
# Legacy path - CRs now stored in docs/CRs/
# server/tasks
**/debug-tasks/
server/sample-tasks
playwright-report
test-results
.amazonq
.mcp.json

lambda-counter-aws
generated-docs/repomix_output.xml

# Counter API - moved out of main codebase
counter-api/

# TypeScript incremental compilation
*.tsbuildinfo
.claude
# ignore SCIP files
**/*.scip
# suppress unused file copies
**/.unused
**/unused.*
**/typescript-graph.md
# MDT agentic implementation checkpoint state
**/.*.json
**/.*.yaml
# Pipeline state tracking (ignore in any subfolder)
**/pipeline-state.json
/.*/
!/.agents/
!/.agents/skills/
/.agents/skills/*
!/.agents/skills/mdt-pipeline-e2e/
!/.agents/skills/mdt-pipeline-e2e/**
/.agents/skills/*/
!/.agents/skills/mdt-pipeline-e2e/

# Task status tracking
*.tasks-status.yaml
.checkpoint.yaml

# debug files
_*
designs/

# debug spec-trace files
spec-trace*.md
**/*.trace.md
*.bun-build
/*.md
/*.txt
incidents/
nohup.out

# ignore agent prompts
**/*prompt*.md

# pipeline json
**/*.pipeline-state.json
lefthook-local.yml

# ITCSS nightly audit working files (agent state + reports, not durable docs)
# The style guide itself (src/ITCSS.md) IS tracked; only working state is ignored.
src/ITCSS-reports/
src/ITCSS-plan.md
src/ITCSS-migration-map.md

# ── Project-wide patterns (migrated from .git/info/exclude) ──
# Proof-of-concept dirs and scratch test files (convention, not shared code)
**/poc/**
# Backup files
**/*.bak.*
# Generated report / summary files (not source of truth)
**/*-report-*.md
**/*-report.md
**/*-summary.md
# Tauri experiment (rejected — see distribution-update-system-plan memory)
src-tauri/
# Binary build artifact
server/server.binary.ts
build-bun-binary.sh
# Secret keys
.security-key
logs/security/
# Local-only design mockups (board-zai parallel design system — design artifact)
docs/design/mockups/wireloom/
scripts/
